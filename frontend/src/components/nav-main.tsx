"use client";
import { SidebarGroup, SidebarMenu } from "@/components/ui/sidebar";
// import FolderOption from "./ui/folder-option";
import React from "react";

export function NavMain() {

  return (
    <SidebarGroup>
      <SidebarMenu>
        {/* <FolderOption folder={[]}/> */}
      </SidebarMenu>
    </SidebarGroup>
  );
}
